apiVersion: v1
kind: Service
metadata:
  name: {{ include "tubadventure.fullname" . }}
  labels:
    {{- include "tubadventure.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "tubadventure.selectorLabels" . | nindent 4 }}
